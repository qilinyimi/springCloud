spring:
  application:
    name: eurekaServer
  profiles:
    active: dev
  cloud:
    config:
      uri: http://loccalhost:8888
      fail-fast: true
      label: master
      name: ${spring.application.name}
      profile: dev
